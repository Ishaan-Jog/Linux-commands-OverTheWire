## Level 3 > Level 4

The password for next level is stored in a hidden file in the `inhere` directory.  
Use `ls -a` to list all the files in the directory, including the hidden ones.
Use `cat` command to read the file.  
Note: Names of hidden files always begin with a dot.  

### Command
```bash
cd inhere
cat ./...Hiding-From-You
```

### Password for next level
`xzTXq1rDJQVVAzdv5cHq1TQytTWufAMq`
