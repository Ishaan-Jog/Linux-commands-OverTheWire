## Level 1 > Level 2

Exit from the current SSH interface and log-in again to Level 1 interface by changing `bandit0` to `bandit1`.  
Use the password obtained in the previous session.

---

The password for the next level is stored in file called `-`.  
To access files with special names, use the prefix `./` which indicates that the file exists in the current working directory.

### Command
```bash
cat ./-
```

### Password for next level
`PK8fYLZg2hnHSz83plBL1iEPKdD3QToB`
